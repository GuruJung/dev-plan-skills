interface:
  display_name: "Save Approved Plan"
  short_description: "Persist a finalized feature plan without implementation"
  default_prompt: "Use $save-approved-plan to persist the latest finalized feature plan without implementing it."

policy:
  allow_implicit_invocation: false
