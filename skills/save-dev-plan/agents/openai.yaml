interface:
  display_name: "Save Dev Plan"
  short_description: "Persist plans and continue approved handoffs"
  default_prompt: "Use $save-dev-plan to persist the latest finalized development plan without implementing it."

policy:
  allow_implicit_invocation: true
